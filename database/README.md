# Database

Esta carpeta queda reservada para futuros scripts de SQL Server asociados a Nuam Exchange.

El modelo físico, tablas, relaciones, índices, restricciones, scripts y migraciones se implementarán en un prompt posterior. No existen scripts SQL en esta entrega.
