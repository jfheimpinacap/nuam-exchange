# 02 — Modelo base de datos SQL Server

Se reserva el nombre de base de datos futura `NuamTributariaDB`.

SQL Server 2022 será el motor definido para la etapa de persistencia. En esta entrega no existen tablas, relaciones, índices, restricciones, scripts ni migraciones.

El modelo físico se implementará en una etapa posterior.
