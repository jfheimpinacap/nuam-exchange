import { Button } from '../../components/Button';
import { FormField } from '../../components/FormField';
import type { DashboardFilters as Filters } from '../../types/dashboard';
export function DashboardFilters({ draft, active, onChange, onApply, onClear }: { draft: Filters; active: Filters; onChange: (f: Filters)=>void; onApply:()=>void; onClear:()=>void }) { return <section className="filters-panel" aria-label="Filtros del dashboard"><FormField id="dashboard-year" label="Ejercicio"><select id="dashboard-year" value={draft.year} onChange={(e)=>onChange({...draft, year:e.target.value as Filters['year']})}><option>Todos</option><option>2024</option><option>2025</option><option>2026</option></select></FormField><FormField id="dashboard-market" label="Mercado"><select id="dashboard-market" value={draft.market} onChange={(e)=>onChange({...draft, market:e.target.value as Filters['market']})}><option>Todos</option><option>Acciones</option><option>Renta Fija</option><option>Fondos</option></select></FormField><div className="filter-actions"><Button variant="primary" onClick={onApply}>Aplicar</Button><Button onClick={onClear}>Limpiar</Button></div><p className="active-period" aria-live="polite">Período activo: ejercicio {active.year}, mercado {active.market}.</p></section>; }
