import type { UploadParsedRow } from '../../types/upload';
export function UploadSummary({ rows }: { rows: UploadParsedRow[] }){ const total=rows.length; const valid=rows.filter((r)=>r.status==='valid').length; const invalid=total-valid; const pct=total?Math.round(valid*100/total):0; return <dl className="upload-summary"><div><dt>Total</dt><dd>{total}</dd><span>Total de registros</span></div><div><dt>Válidos</dt><dd>{valid}</dd><span>Registros válidos</span></div><div><dt>Con error</dt><dd>{invalid}</dd><span>Registros con error</span></div><div><dt>Porcentaje válido</dt><dd>{pct} %</dd><span>Proporción aceptada</span></div></dl>; }
