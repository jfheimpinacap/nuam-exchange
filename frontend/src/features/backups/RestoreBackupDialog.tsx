import { useEffect, useRef, useState } from 'react';
import { Button } from '../../components/Button';
import { FormField } from '../../components/FormField';
import type { BackupRecord } from '../../types/backup';
import { formatBackupDate, formatBytes } from './backupUtils';
export function RestoreBackupDialog({ backup, onClose, onConfirm }:{backup:BackupRecord; onClose:()=>void; onConfirm:()=>void}){ const input=useRef<HTMLInputElement>(null); const [text,setText]=useState(''); useEffect(()=>{input.current?.focus(); const k=(e:KeyboardEvent)=>{if(e.key==='Escape')onClose();}; window.addEventListener('keydown',k); return()=>window.removeEventListener('keydown',k);},[onClose]); return <div className="dialog-backdrop"><section className="confirm-dialog admin-dialog" role="dialog" aria-modal="true" aria-labelledby="restore-title"><h2 id="restore-title">Simular restauración</h2><p className="inline-message inline-warning">Esta acción es solo visual. No modifica datos reales ni mockeados.</p><dl className="detail-list"><div><dt>ID</dt><dd>{backup.id}</dd></div><div><dt>Tipo</dt><dd>{backup.tipo}</dd></div><div><dt>Alcance</dt><dd>{backup.alcance}</dd></div><div><dt>Fecha</dt><dd>{formatBackupDate(backup.fechaInicio)}</dd></div><div><dt>Tamaño</dt><dd>{formatBytes(backup.tamañoBytes)}</dd></div></dl><FormField id="restore-confirm" label="Escriba RESTAURAR para habilitar"><input ref={input} id="restore-confirm" value={text} onChange={e=>setText(e.target.value)} /></FormField><div className="filter-actions"><Button variant="primary" disabled={text!=='RESTAURAR'} onClick={onConfirm}>Confirmar simulación</Button><Button onClick={onClose}>Cancelar</Button></div></section></div>; }
