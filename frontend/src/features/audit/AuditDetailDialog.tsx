import { useEffect, useRef } from 'react';
import { Button } from '../../components/Button';
import type { AuditEvent } from '../../types/audit';
import { formatDateTime } from './auditUtils';
export function AuditDetailDialog({ event, onClose }: { event: AuditEvent; onClose:()=>void }) { const close=useRef<HTMLButtonElement>(null); useEffect(()=>{ close.current?.focus(); const key=(e:KeyboardEvent)=>{ if(e.key==='Escape') onClose();}; window.addEventListener('keydown',key); return()=>window.removeEventListener('keydown',key);},[onClose]); const items=[['ID',event.id],['Fecha y hora',formatDateTime(event.fechaHora)],['Usuario',event.usuarioNombre],['Rol',event.rol],['Módulo',event.modulo],['Acción',event.accion],['Entidad',event.entidad],['Entidad ID',event.entidadId],['Resultado',event.resultado],['Severidad',event.severidad],['Origen',event.origen],['Descripción',event.descripcion]]; return <div className="dialog-backdrop"><section className="confirm-dialog admin-dialog" role="dialog" aria-modal="true" aria-labelledby="audit-detail-title"><h2 id="audit-detail-title">Detalle de evento</h2><dl className="detail-list">{items.map(([k,v])=><div key={k}><dt>{k}</dt><dd>{v}</dd></div>)}{Object.entries(event.detalles).map(([k,v])=><div key={k}><dt>{k}</dt><dd>{v}</dd></div>)}</dl><Button ref={close} onClick={onClose}>Cerrar</Button></section></div>; }
