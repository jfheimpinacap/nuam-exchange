export function ConfirmDialog({ open, title, children, onConfirm, onCancel }: { open:boolean; title:string; children:React.ReactNode; onConfirm:()=>void; onCancel:()=>void }) { if(!open) return null; return <div className="dialog-backdrop"><section className="dialog"><h2>{title}</h2><div>{children}</div><footer><button type="button" onClick={onCancel}>Cancelar</button><button type="button" onClick={onConfirm}>Confirmar</button></footer></section></div>; }
